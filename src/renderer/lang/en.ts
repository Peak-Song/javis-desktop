import { I18nRouter, I18nLocale } from './index.type'

const i18nRouter: I18nRouter = {
  Note: 'Note',
  Dashboard: 'Dashboard'
}

const en: I18nLocale = {
  route: i18nRouter
}

export default { en }
